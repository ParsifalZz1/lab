# ModelFlow MVP 开发任务清单

## 1. 使用方式

本文件是从空仓库到可演示 MVP 的执行顺序，而不是愿望列表。

每一阶段完成其验收条件后，再进入下一阶段。

出现实现分歧时，优先级依次为：`goal.md`、架构文档、本文、局部实现便利性。

阅读顺序：

1. `goal.md`
2. `ARCHITECTURE.md`
3. `DAG_ARCHITECTURE.md`
4. `MODEL_REGISTRY.md`
5. `DEVELOPMENT_GUIDE.md`
6. 本文及对应阶段的接口、协议、测试文档

## 2. MVP 完成定义

MVP 必须支持用户提交一个文本任务，并由 Brain 生成合法 DAG。

系统必须从 Registry 选择至少 8 个可用 Worker，执行无依赖节点并发分派。

系统必须支持结构化结果归约、超时、重试、取消和运行状态查询。

Web Demo 必须展示 DAG、Worker 状态、任务时间线、最终结果与加速比。

MVP 的首个固定场景为“电商评论分批分析并生成改进建议”。

## 3. 明确不做

- 训练、微调、蒸馏或联邦学习。
- 多模态输入与多模态模型。
- 跨地域强一致的多控制面集群。
- 任意第三方模型厂商的全量适配。
- 基于强化学习的自动调度优化。
- 在未完成文本 PoC 前建设通用工作流平台。

## 4. 实施规则

- 每个阶段先写契约与测试，再写实现。
- 保持 `Run -> DAG -> Scheduler -> Worker -> Reducer` 单向主链路。
- 所有跨模块对象必须能 JSON 序列化并带版本。
- Worker 不得读取全局数据库或修改 DAG 状态。
- 先使用可控的 Mock Worker 验证流程，再接入真实模型。
- 每项任务完成时更新本文件中的复选框和验收记录。

## 5. Phase 0：项目骨架

目标：建立可运行、可测试、可检查的最小工程。

- [x] 按 `DEVELOPMENT_GUIDE.md` 创建后端、前端、测试和文档目录。
- [x] 固定语言版本、依赖管理方式、格式化、静态检查和测试命令。
- [x] 配置环境变量模板，不提交密钥或本机地址。
- [x] 建立 `GET /healthz` 和最小应用启动测试。
- [x] 建立统一配置对象、日志格式与 `trace_id` 中间件。
- [x] 建立本地 SQLite 开发数据库和迁移机制。

验收：全新环境可按开发指南启动服务、运行格式检查和通过基础测试。

## 6. Phase 1：领域模型与持久化

目标：先固定运行状态和可恢复的数据边界。

- [x] 实现 `Run`、`DagSnapshot`、`TaskNode`、`TaskAttempt` 与 `Artifact` 数据模型。
- [x] 实现 `WorkerRecord`、`CapabilityRecord` 和 `Lease` 数据模型。
- [x] 实现状态枚举和合法状态迁移检查。
- [x] 为 `run_id`、`dag_id`、`task_id`、`attempt_id` 建立稳定 ID 生成器。
- [x] 实现事件表和 Outbox 写入接口。
- [x] 为运行查询、任务查询和节点查询建立索引。

验收：可创建一个 Run、保存一个静态 DAG、写入状态事件，并在重启后读取完整快照。

参考：`DATA_AND_EVENTS.md`。

## 7. Phase 2：模型注册中心

目标：让 Worker 可动态加入、续约、离开，并能被能力查询。

- [x] 实现节点注册、更新、心跳、drain 和注销 API。
- [x] 校验 `worker_id`、Capability 版本、端点协议与容量字段。
- [x] 实现租约过期扫描和 `SUSPECT`/`OFFLINE` 状态迁移。
- [x] 实现按角色、能力、版本、地域和状态过滤的候选查询。
- [x] 实现活动任务数和队列深度的负载写入。
- [x] 为 Registry 状态变化写入事件。
- [x] 提供本地脚本或 Mock Worker 自动注册 8 个节点。

验收：停止一个 Mock Worker 的心跳后，它在租约到期后不再出现在候选集中；重启后可重新注册。

参考：`MODEL_REGISTRY.md`、`API_CONTRACT.md`。

## 8. Phase 3：DAG 生成与校验

目标：将用户目标变为可信、可执行的任务图。

- [x] 定义 DAG、Node、Edge、InputRef、RetryPolicy 的 Pydantic/JSON Schema。
- [x] 实现节点 ID 唯一、依赖存在、输入引用、预算和能力字段校验。
- [x] 使用拓扑排序检测环路并计算初始 ready 节点集合。
- [ ] 实现固定模板的评论分析 DAG Generator。
- [ ] 定义 Brain Planner 接口，并先用确定性 Fake Brain 返回模板 DAG。
- [ ] 保存不可变 DAG Snapshot 和校验错误列表。
- [ ] 仅在静态校验通过后允许创建 TaskNode 运行时记录。

验收：合法示例能生成拓扑层；缺失依赖、循环依赖和错误输入引用均被拒绝并返回机器可读错误。

参考：`DAG_ARCHITECTURE.md`、`EXECUTION_PROTOCOL.md`。

## 9. Phase 4：调度与执行器

目标：让 ready 节点依据能力和负载被并发执行。

- [ ] 实现候选硬过滤：在线、租约、能力、版本、上下文窗口和容量。
- [ ] 实现基线策略：能力匹配后选择最少活动任务的 Worker。
- [ ] 写入包含快照版本和选择理由的 Assignment。
- [ ] 实现有界并发 Execution Coordinator。
- [ ] 实现标准 TaskEnvelope、截止时间、幂等键和取消令牌。
- [ ] 实现 HTTP Mock Worker Adapter 与统一 TaskResult 解析。
- [ ] 实现成功、超时、可重试失败、不可重试失败和拒绝过载的处理。
- [ ] 实现同一节点优先重分配到不同 Worker 的重试。

验收：8 个独立批次在容量允许时并发启动；一个 Worker 超时后任务被另一 Worker 重试；独立分支继续运行。

参考：`EXECUTION_PROTOCOL.md`、`DAG_ARCHITECTURE.md`。

## 10. Phase 5：依赖推进与归约

目标：让 DAG 状态随结果自动前进，并产出可追溯的上下文包。

- [ ] 在 attempt 终态后原子更新 TaskNode 状态和下游依赖计数。
- [ ] 实现 `PENDING -> READY -> SCHEDULED -> RUNNING -> terminal` 状态机。
- [ ] 实现 optional 节点失败后的缺失对象与降级事件。
- [ ] 实现确定性 Reducer：字段合并、去重、排序、频次统计和来源保留。
- [ ] 为最终结果定义 Context Package 与 Artifact 存储。
- [ ] 用 Fake Brain 将 Context Package 转为 MVP 报告。
- [ ] 实现 Run 终态判定和取消传播。

验收：多批评论结果可合并为 Top 问题、证据和建议；每项输出可回溯到 task 与 attempt。

## 11. Phase 6：对外 API 与事件流

目标：提供稳定的调用接口和面向 Demo 的实时状态。

- [ ] 实现创建 Run、查询 Run、取消 Run、查询 DAG 和查询任务 API。
- [ ] 实现 SSE 事件流，支持按 `run_id` 从事件序号续接。
- [ ] 实现请求校验、统一错误响应、分页和幂等创建。
- [ ] 将每次 API、调度和执行行为关联到 `trace_id`。
- [ ] 完成 OpenAPI 文档与示例请求。

验收：客户端可以只依赖 HTTP API 完成提交、订阅进度、读取最终结果和取消运行。

参考：`API_CONTRACT.md`。

## 12. Phase 7：真实模型与 Web Demo

目标：替换最小路径中的模拟组件，呈现模型互联网运行过程。

- [ ] 接入一个真实 Brain 和至少一种真实 Worker Adapter。
- [ ] 保留 Mock Worker，确保 CI 与本地开发不依赖外部模型密钥。
- [ ] 构建任务输入、运行列表、DAG、Worker 拓扑、事件时间线和结果视图。
- [ ] 显示节点选择、尝试次数、耗时、状态和失败原因。
- [ ] 实现串行基线与并行运行的可比较耗时记录。
- [ ] 显示加速比、并发度、成功率与降级说明。

验收：演示人员无需查看服务日志，即可理解任务从 DAG 生成到结果归约的完整过程。

## 13. Phase 8：稳定性与演示验收

目标：在展示前验证功能、失败路径与性能表述。

- [ ] 完成 `TEST_PLAN.md` 中标记为 MVP 必须项的测试。
- [ ] 进行 Worker 下线、慢节点、格式错误、重复结果和取消场景演练。
- [ ] 固定演示数据集、随机种子和串行基线配置。
- [ ] 记录 8 Worker 并发场景的耗时、失败率和加速比。
- [ ] 检查日志中无密钥、原始敏感输入或未关联异常。
- [ ] 编写演示操作步骤和故障回退方案。

验收：所有核心路径可重复通过；性能结论由同一输入和明确基线支撑。

## 14. 每日开发节奏

开始当天：从本阶段最上方未完成任务开始，先补测试或契约。

实现中：每完成一个跨模块对象，检查它是否有版本、ID、状态和错误语义。

结束前：运行格式检查、相关测试和端到端最短路径；更新复选框与阻塞原因。

不要跳过 Phase 1 至 5 直接制作可视化页面；页面只展示已持久化的真实状态。

## 15. 开始开发前的唯一决策

本仓库尚未固定技术栈。若无额外约束，采用 `DEVELOPMENT_GUIDE.md` 的推荐基线。

若要改用其他语言、消息系统或前端框架，应先更新开发指南与对应接口测试，不改变领域协议。
